# Benchmarking script

The script shows real-time accuracy as it processes each image and displays a final summary report.

## Setup

> [!Important]
> Make sure the python backend is running ([see instructions](captcha-endpoint/backend/README.md))

```bash
pnpm install
pnpm test
```
